name: Build Debug APK

on:
  push:
    branches:
      - main
    paths-ignore:
      - "multiplatform/**"
      - "docs/**"
      - "**/*.md"
      - ".github/workflows/kmp-package.yml"
  workflow_dispatch:

concurrency:
  group: debug-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-debug-apk:
    name: Build Debug APK
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up JDK 17
        uses: actions/setup-java@v6
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Use Google/Maven Central on GitHub runners
        run: python3 .github/ci/rewrite_ci_maven_repos.py

      - name: Build Debug APK
        run: ./gradlew assembleDebug --init-script .github/ci/github-maven-fallback.init.gradle.kts --stacktrace

      - name: Upload Debug APK
        uses: actions/upload-artifact@v7
        with:
          name: app-debug-apk
          path: app/build/outputs/apk/debug/app-arm64-v8a-debug.apk
